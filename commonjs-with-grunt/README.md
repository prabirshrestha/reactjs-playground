## React JS sample using CommonJS pattern & Grunt

To create `public/app.js` file run the following command.

```bash
npm install
grunt
```

Then open `http://localhost:3000/` in the browser which serves the `/public` folder.
